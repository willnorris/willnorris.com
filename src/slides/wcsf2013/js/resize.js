var Resize = (function() {
    var config = {
        width: 1024,
        height: 768,
    };


    function layout() {
        var slides = document.getElementsByTagName("screens")[0];
        var availableWidth = slides.offsetWidth;
        var availableHeight = slides.offsetHeight;
    }
})();
